export default function DispensePage() {
  return (
    <div className="p-8 max-w-4xl mx-auto text-center">
      <h1 className="text-2xl font-bold text-slate-800 mb-2">Dispensing Panel</h1>
      <p className="text-gray-500">The FEFO engine is active in the backend (`dispensingService`). UI will be wired up to Prescriptions here.</p>
    </div>
  )
}
